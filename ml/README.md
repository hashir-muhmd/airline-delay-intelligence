# ML

Training scripts and saved model artifacts.

Planned contents:
- `train_classifier.py` — delayed / not delayed
- `train_regressor.py` — predicted delay duration
- `cascade_model.py` — downstream disruption propagation
- `forecasting.py` — Prophet-based seasonal delay trends
- `artifacts/` — saved model files (gitignored, not committed)

Status: not yet implemented.
